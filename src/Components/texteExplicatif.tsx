import React from 'react';
import { plat } from '../tempAvantFusion/variables.tsx';

const TexteExplicatif: React.FC = () => {
    return <p>Vous allez composer votre menu avec {plat} et 2 accompagnements.</p>;
};

export default TexteExplicatif;
